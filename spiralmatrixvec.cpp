#include<iostream>
#include<vector>
using namespace std;

/*class Solution{
public:
vector<int> spiralOrder(vector<vector<int>>&matrix)
{
    int m=matrix.size();
    int n=matrix[0].size();
    vector<int>res;
    int sr,er,sc,ec,cnt=0;
    sr=0;sc=0;
    er=m-1;ec=n-1;
    while(sr<=er && sc<=ec)
    {
        ///
        for(int i=sc;i<=ec;++i){
            res.push_back(matrix[sr][i]);
            //cnt++;
        }
        sr++;
        //if(cnt==m*n)return res;
        ///
        for(int i=sr;i<=er;++i){
            res.push_back(matrix[i][ec]);
            //cnt++;
        }
        ec--;
        //if(cnt==m*n)return res;
        ///
        for(int i=ec;i>=sc;--i){
            res.push_back(matrix[er][i]);
            //cnt++;
        }
        er--;
        //if(cnt==m*n)return res;
        ///
        for(int i=er;i>=sr;--i){
            res.push_back(matrix[i][sc]);
            //cnt++;
        }
        sc++;
        //if(cnt==m*n)return res;
    }
    return res;
}
};*/
int main()
{
    /*Solution s;
    vector<vector<int>>matrix;
    int m,n,num;
    cout<<"ENter rows and columns:"<<endl;
    cin >> m >> n;
    cout<<"Enter matrix elements:"<<endl;
    for(int i=0;i<m;i++)
        {
            vector<int>temp;
            for(int j=0;j<n;j++)
                {
                    cin >> num;
                    temp.push_back(num);
                }
                matrix.push_back(temp);
        }
        cout<<"Original Vector:"<<endl;
        for(int i=0;i<m;i++){
            for(int j=0;j<n;j++)
                cout << matrix[i][j]<<" ";
            cout<<endl;
        }
        vector<int>ans;
        ans=s.spiralOrder(matrix);
        ///printing the vector
        cout<<"Resultant vector:"<<endl;
        for(int i=0;i<ans.size();i++)
            cout<<ans[i]<<" ";*/
            cout<<"Kakali Bhattacharya is my mother";

}





